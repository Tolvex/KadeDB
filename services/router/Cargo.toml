[package]
name = "kadedb-services-router"
version = "0.1.0"
edition = "2021"

[dependencies]
kadedb-services-grpc = { path = "../grpc" }
serde_json = "1"
thiserror = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
tonic = "0.12"

[dev-dependencies]
kadedb-services-auth = { path = "../auth" }
